import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // Private constructor to prevent instantiation

  // Brand colors
  static const Color primaryColor = Color(0xFF1D9E75);
  static const Color secondaryColor = Color(0xFF0F6E56);

  // Neutral colors
  static const Color white = Color(0xFFFEFEFE);
  static const Color black = Color(0xFF000000);
  static const Color blue = Color(0xFF386BFA);
  static const Color grey = Color(0xFFABA7A7);

  // Background colors
  static const Color backgroundLight = Color(0xFFF2F6FC);

  // Text colors
  static const Color textPrimary = Color(0xFF3D3B3B);
  static const Color textSecondary = Color(0xFFA2AED0);
  static const Color textWhite = Color(0xFFfefefe);
  static const Color textLink = Color.fromARGB(255, 124, 156, 244);
}
